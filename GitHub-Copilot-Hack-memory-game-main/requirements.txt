Flask==2.3.2               # Web framework to handle HTTP requests and serve the game
Flask-CORS==3.0.10         # To handle Cross-Origin Resource Sharing if frontend and backend are separate
requests==2.31.0           # For handling HTTP requests (if needed to fetch external data)
